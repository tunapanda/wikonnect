const log = {
  info: (scope, message) => {
    console.log(scope, message);
  },
  error: (scope, message) => {
    console.log(scope, message);
  },
  warn: (scope, message) => {
    console.log(scope, message);
  },
};

export { log };
