{{outlet}}

<div class="container">

  <div class="row m-5">
    {{#if model}}


      {{#each model as |chapter|}}

        <H5pListItem @chapter={{chapter}} />
      {{/each}}
    {{else}}
      {{t "messages.empty_search"}}

    {{/if}}

  </div>
</div>