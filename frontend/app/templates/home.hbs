<div class="container">
  <TagsList @theModel={{model}} />

  <div class="row mt-5 ">
    {{#each model as |chapter|}}

      <H5pListItem @chapter={{chapter}} @reactionNotify={{this.reactionFeedback}}/>
    {{/each}}
  </div>
</div>